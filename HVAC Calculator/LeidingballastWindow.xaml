<Window x:Class="HVACCalculator.LeidingballastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Source={x:Static Application.Current}, Path=Resources[AppVersion], StringFormat=Leidingballast - Versie {0}}"
        SizeToContent="WidthAndHeight"
        Background="#FF002262"
        WindowStartupLocation="Manual"
        ResizeMode="CanMinimize"
        Topmost="True"
        Icon="/Resources/VA.ico">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Width" Value="120"/>
            <EventSetter Event="GotFocus" Handler="TextBox_GotFocus"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5,20,5,2"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="#FF0264C9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
        <Style x:Key="RowActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="#FF1D8BF0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF0264C9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="24"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="IsEditable" Value="True"/>
            <EventSetter Event="GotFocus" Handler="ComboBox_GotFocus"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" Width="820">
            <TextBlock Text="Leidingballast" Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Vul per leidingdeel materiaal, diameter en lengte in. Gewicht wordt automatisch berekend op basis van de gevulde buis." Style="{StaticResource LabelStyle}" Margin="0,0,0,6" FontSize="12" TextWrapping="Wrap"/>

            <DataGrid x:Name="ballastGrid"
                      ItemsSource="{Binding BallastRows}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      Background="#FF001A4D"
                      RowBackground="#FF001A4D"
                      AlternatingRowBackground="#FF002262"
                      Foreground="White"
                      BorderBrush="#FF0264C9"
                      BorderThickness="1"
                      HorizontalScrollBarVisibility="Disabled"
                      CanUserReorderColumns="False"
                      CanUserResizeColumns="False"
                      CanUserSortColumns="False"
                      Margin="0,2,0,8"
                      Height="340"
                      TabIndex="1"
                      KeyboardNavigation.TabNavigation="Local">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF0264C9"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="6,4"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="40" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Leidingmateriaal" Width="190">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.AvailableMaterials, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding SelectedMaterial, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          MinWidth="170" Height="24" Margin="2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Diameter [mm]" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding AvailableDiameters}"
                                          SelectedItem="{Binding SelectedPipe, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          DisplayMemberPath="OuterDiameterMm"
                                          MinWidth="100" Height="24" Margin="2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Lengte [m]" Binding="{Binding LengthM, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
                    <DataGridTextColumn Header="kg/m" Binding="{Binding WeightPerM}" Width="80" IsReadOnly="True"/>
                    <DataGridTextColumn Header="kg" Binding="{Binding RowWeightKg}" Width="80" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="+" Width="35">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="+" Style="{StaticResource RowActionButtonStyle}" Click="AddRow_Click" Focusable="False"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="-" Width="35">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="-" Style="{StaticResource RowActionButtonStyle}" Click="RemoveRow_Click" Focusable="False"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Grid Margin="0,3,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="45"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Totale leidinglengte" Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Column="1" x:Name="tbTotalLength" IsReadOnly="True" Background="#FF333333" Foreground="White" Width="140"/>
                <TextBlock Grid.Column="2" Text="m" Style="{StaticResource LabelStyle}"/>

                <TextBlock Grid.Column="3" Text="Totaal leidinggewicht" Style="{StaticResource LabelStyle}"/>
                <TextBox Grid.Column="4" x:Name="tbTotalWeight" IsReadOnly="True" Background="#FF333333" Foreground="White" Width="140"/>
                <TextBlock Grid.Column="5" Text="kg" Style="{StaticResource LabelStyle}"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5,0,0,0">
            <Button Content="Wissen" Click="btnWissen_Click" Margin="5,2"/>
            <Button Content="Afsluiten" Click="btnAfsluiten_Click" Margin="5,2"/>
            <Button Content="Info" Click="btnInfo_Click" Margin="5,2"/>
            <Button Content="Export" Click="btnExport_Click" Margin="5,2"/>
        </StackPanel>
    </Grid>
</Window>
